# Docker 构建上下文排除（与 .gitignore 不完全相同——避免破坏 docker build 缓存）

# Git
.git/
.gitignore
.gitattributes

# GitHub
.github/

# 大文件 / 数据集 / 模型
dataset/*/images/
dataset/*/labels/
dataset/**/*.jpg
dataset/**/*.jpeg
dataset/**/*.png
dataset/**/*.txt
dataset/**/*.cache
basemodels/*.pt
exports/*.pt
runs/
logs/
reports/
tuning/
model_registry/
Ultralytics/

# CI 缓存
.ci/ultralytics/
.ci/matplotlib/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/

# Venv
.venv/
venv/
env/

# 构建产物
build/
dist/
*.egg-info/

# 编辑器 / OS
.idea/
.vscode/
*.code-workspace
.DS_Store
Thumbs.db
Desktop.ini

# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# 临时文件
*.tmp
*.temp
*.bak
*.swp
*~$
*.log

# 文档与构建脚本（Dockerfile 不需要；docs 由 MkDocs 工作流构建）
docs/
scripts/
*.md

# ⚠️ 下面这批 ! 例外必须留在 *.md 之后：Docker 取「最后一条命中的模式」，
#    放在 *.md 之前会被 *.md 重新排除，于是 COPY README.md 直接构建失败。
#    README.md 不是可有可无的：pyproject.toml 的 readme 字段指向它，
#    缺了它 setuptools 构建元数据会报错。
!Dockerfile
!pyproject.toml
!README.md
!constraints.txt
!requirements.txt
!requirements-dev.txt

# 前端依赖与产物不进上下文：
#   - node_modules 有 5500+ 文件，灌进去会让上下文膨胀且拖慢每次构建；
#     镜像内的依赖由 Dockerfile 的 frontend stage 用 pnpm --frozen-lockfile 重装
#   - dist 是本地陈旧产物，镜像内必须现构建，否则会发布过期的界面
frontend/node_modules/
frontend/dist/

# 注意：test/ 不在本文件中，因为 Dockerfile 会 COPY test/ 进镜像。
#       排除它会导致同样的 "excluded by .dockerignore" 构建失败。